# Основные возможности:

* **Гибридный мониторинг:** Одновременная работа через WebSockets и Polling для минимальной задержки.
* **Гибкие фильтры:** Настройка минимальной и максимальной суммы (Min/Max).
* **Скорость:** Оптимизированные TCP-соединения, uvloop и конкурентные запросы.
* **Аналитика:** Ежедневные отчеты и статистика пойманных объемов.
* **Безопасность:** Поддержка HTTP-прокси и безопасное хранение токенов.

---

## Как достичь большей скорости?

Сейчас в репозитории вы видете легкий наработок бота. Если вы хотите получить большую скорость, то у вас есть возможность приобрести P2C-снипер на **C++** и **Goland**. Ловит новые платежи в реальном времени, фильтрует по сумме, параллельно делает тейк, ведет статистику, отправляет уведомления в тг (включая QR и ссылку), поддерживает удаленные действия complete/cancel через тг коллбеки и имеет множетсво технических парамтеров, которые за бесплатно не разглошаются. Недавно вышло крупное обновление в котором скорость была значительно увеличенна. 

**Что можно купить отдельно:**
1. Исходный код полноценнного софта — **200$**.

2. Название 4 серверов *в одном их них предположительно развернут @send* и регион. Пинг: **0.4мс** — **75$**
3. Список подсказок, рекомендаций, сетевых наблюдений, предположительный origin, идеи по оптимизации и прочая информация, которая добывалась около **7 месяцев**. — **100$**. Если берешь этот пакет, то дополнительно получаешь:
    - 1 версию на C++ бесплатно
    - право задать тех. вопросы по этой теме
    - базовые пояснения, куда копать дальше и что вообще имеет смысл тестировать

Полное приобретение софта: **400$**. Остерегайтесь скамеров, которые продают софт по низкой цене! С большим шансом вы получите открытый код из этого репозитория.

**Купить или задать вопросы:** [Telegram @daich](https://t.me/daich). Сам я не пользуюсь софтом, т.к мне это не надо. Раньше работал в команде, но из-за различия взглядов на аудиторию и как результат ссоры покинул пост и удалил все с серверов. 

---

# Установка на сервер

## Подготовка окружения

Обновляем систему и ставим необходимые пакеты (Python, Git, Rust для быстрой компиляции библиотек):

```bash
apt update && apt upgrade -y
apt install python3-pip python3-venv git pkg-config libssl-dev -y

curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
source "$HOME/.cargo/env"
```

## Установка бота

Клонируем репозиторий и ставим зависимости:

```bash
git clone https://github.com/ReNothingg/P2C-Crypto-Sniper-Bot.git
cd P2C-Crypto-Sniper-Bot

# Создаем виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Ставим библиотеки
pip3 install -r requirements.txt
```

### Первый запуск

Запуск с высоким приоритетом процесса (требует прав root):

```bash
sudo taskset -c 1 chrt -f 99 venv/bin/python3 main.py
```

*После запуска перейдите в Telegram-бота и нажмите /start для настройки токена и лимитов.*


---

# ОТКАЗ ОТ ОТВЕТСТВЕННОСТИ

> **Данное ПО разработано исключительно в ознакомительных целях. Разработчик не несет ответственности за блокировки аккаунтов или финансовые потери. Используйте на свой страх и риск.**

По законам многих стран автоматизация крипты и P2С — это серая зона или откровенная уголовка. Скачивая этот код, ты берешь все риски на себя. Если за тобой приедет патруль из-за перехваченного чека на 50 рублей и конфискует твою клавиатуру — я тебя предупреждал.

Этот код распространяется под **гибридной лицензией**. Правила простые:

1. COPYLEFT (ОБЯЗАН ДЕЛИТЬСЯ): Если ты взял мой бесплатный Python-код, допилил его и используешь — ты обязан выложить свои изменения в открытый доступ. Никаких «приватных сборок» на моем фри-коде. Взял бесплатно — отдай бесплатно.

2. AS IS (БЕЗ ГАРАНТИЙ): Софт предоставляется «КАК ЕСТЬ». Если из-за этого скрипта у тебя списали баланс, забанили аккаунт, сгорел дата-центр или мама забрала у тебя банковскую карточку — ЭТО ТВОИ ЛИЧНЫЕ ПРОБЛЕМЫ.

Запуская скрипт, ты соглашаешься с тем, что сам отвечаешь за свои действия и свою жопу.
